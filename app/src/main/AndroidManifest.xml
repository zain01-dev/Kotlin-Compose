<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jpcompose">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JPCompose">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JPCompose.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".text.SimpleTextActivity" />
        <activity android:name=".text.EditTextFieldActivity" />
        <activity android:name=".layout.LazyColumnActivity" />
        <activity android:name=".layout.RowActivity" />
        <activity android:name=".layout.BoxActivity" />
        <activity android:name=".layout.ConstraintLayoutActivity" />
        <activity android:name=".card.CardActivity" />
        <activity android:name=".button.ButtonActivity"/>
        <activity android:name=".image.ImageActivity"/>
        <activity android:name=".layout.ScrollableColumnActivity"/>
        <activity android:name=".dialog.AlertDialog"/>
        <activity android:name=".materialDesign.MaterialActivity"/>
        <activity android:name=".layout.PaddingActivity"/>
        <activity android:name=".layout.ComposeInClassicAndroidActivity"/>
    </application>

</manifest>